FILE = proj5
BIN = /usr/local/share/texlive/bin
PS2PDFBIN = /usr/bin

all: $(FILE).tex
	$(BIN)/pdflatex $<
	$(BIN)/pdflatex $<

clean:
	rm $(FILE).dvi
	rm $(FILE).ps
	rm $(FILE).pdf
	rm $(FILE).log
	rm $(FILE).aux
