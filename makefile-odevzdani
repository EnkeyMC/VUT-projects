FILE = proj4

all: $(FILE).ps
	ps2pdf $<

$(FILE).ps: $(FILE).dvi
	dvips -t a4 $<

$(FILE).dvi: $(FILE).tex
	latex $<
	bibtex $<
	latex $<
	latex $<

clean:
	rm $(FILE).dvi
	rm $(FILE).ps
	rm $(FILE).pdf
	rm $(FILE).log
	rm $(FILE).aux
