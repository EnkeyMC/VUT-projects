FILE = proj5

all: $(FILE).ps
	ps2pdf $<

$(FILE).ps: $(FILE).dvi
	dvips $<

$(FILE).dvi: $(FILE).tex
	latex $<
	latex $<

clean:
	rm $(FILE).dvi
	rm $(FILE).ps
	rm $(FILE).pdf
	rm $(FILE).log
	rm $(FILE).aux
