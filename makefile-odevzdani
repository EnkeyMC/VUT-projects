FILE = proj4

all: $(FILE).ps
	ps2pdf $<

$(FILE).ps: $(FILE).dvi
	dvips -t a4 $<

$(FILE).dvi: $(FILE).tex
	latex $<
	bibtex $(FILE)
	latex $<
	latex $<

clean:
	rm -f $(FILENAME).dvi
	rm -f $(FILENAME).ps
	rm -f $(FILENAME).pdf
	rm -f $(FILENAME).log
	rm -f *.bbl
	rm -f *.blg
