FILE = proj3

all: $(FILE).ps
	ps2pdf -sPAPERSIZE=a4 $<

$(FILE).ps: $(FILE).dvi
	dvips $<

$(FILE).dvi: $(FILE).tex
	latex $<
	latex $<

clean:
	rm $(FILE).dvi
	rm $(FILE).ps
	rm $(FILE).pdf
	rm $(FILE).log
	rm $(FILE).aux
